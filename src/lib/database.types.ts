// Placeholder. Regenerate against your real project with:
//   npx supabase gen types typescript --project-id <PROJECT_ID> > src/lib/database.types.ts
// Left loose here so the scaffold type-checks before you've run that step.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
